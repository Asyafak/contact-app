<header class="bg-transparent absolute top-0 left-0 w-full flex items-center">
  <div class="container">
    <div class="flex items-center relative justify-between w-full">
      <div class="px-4">
        <h1 class="font-bold text-xl py-6 text-primary">Contact App</h1>
      </div>
      <div class="flex items-center px-4 md:hidden">
        <button id="hamburger" type="button" name="hamburger" class="block absolute right-4">
          <span class="hamburger-line transition ease-in-out duration-300 origin-top-left"></span>
          <span class="hamburger-line transition ease-in-out duration-300"></span>
          <span class="hamburger-line transition ease-in-out duration-300 origin-bottom-left"></span>
        </button>
      </div>
      <nav class="scale-0 flex flex-col absolute bg-white shadow right-14 top-14 p-4 min-w-48 max-w-64 rounded gap-4 transition duration-300 origin-top-right md:static md:scale-100 md:flex-row md:bg-transparent md:shadow-none md:rounded-none">
        <a class=" hover:text-primary" href="/">Home</a>
        <a class=" hover:text-primary" href="/contact">Contact</a>
        <a class=" hover:text-primary" href="/groups">Groups</a>
      </nav>
    </div>
  </div>
</header>

<script>
  const hamburger = document.getElementById('hamburger')
  const navBar =document.querySelector('nav')

  hamburger.addEventListener('click', () => {
    hamburger.classList.toggle('hamburger-active')
    navBar.classList.toggle('nav-bar-active')
  })
</script>